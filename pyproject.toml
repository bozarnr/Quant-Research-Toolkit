[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "quant-research-toolkit"
version = "0.1.0"
description = "Clean-room utilities for time-safe factor diagnostics."
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "bozarnr" }]
dependencies = [
  "numpy>=1.24",
  "pandas>=2.0",
]

[tool.setuptools.packages.find]
where = ["src"]
